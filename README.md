# z3-web-browser
To run: ```npm run dev```

To add in ```.env```: ```VITE_GOOGLE_API_KEY```
Can switch between models too

I guess we can add in world facts for the whole map and ask gemini to come up with Z3 solver...

And it would be good if we can ask it to place specific tiles like wheelbarrow vs. mushroom. 
